# Same Supabase project as the admin app
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional: service role key for API routes (e.g. /api/attendance) to write to attendance table.
# If not set, the anon key is used; ensure RLS allows insert/update for your policies.
# SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Semaphore SMS (server-only; used when SMS_BACKEND_URL is not set)
SEMAPHORE_API_KEY=your_semaphore_api_key

# Optional: proxy SMS to this backend (server-only; no CORS, URL not exposed to client).
# When set, POST /api/sms/attendance forwards the body here. Same payload expected.
# Example: https://scanpass.arkwaretech.com/api/sms/attendance
# SMS_BACKEND_URL=https://scanpass.arkwaretech.com/api/sms/attendance
